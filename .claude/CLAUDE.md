When reading source code files or modules, ALWAYS use a language server instead of read or grep tools -- you should also give this instruction to sub-agents.

When you need to perform multiple changes, create a change request and then invoke a sub-agent using the Haiku model, and give them the change request.