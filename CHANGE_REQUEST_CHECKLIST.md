# Change Request Checklist

- [x] CR-01: `long_name_truncated` and `roundtrip` are top-level functions, not subtests
- [x] CR-02: `FilenameToName` has no subtest structure
- [x] CR-03: `long_name_truncated` checks byte length, not rune length
- [x] CR-04: `Parse/multiline` — `Lines` assertion too weak
- [x] CR-05: `Parse/body_excludes_title` — partial content validation only
